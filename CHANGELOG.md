# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 1.1.0 - 2026-09-21

- 修复 V1「设置数据范围」保存在默认请求参数时，统计请求遗漏该筛选并统计全部数据的问题；统计请求现在与原生列表使用相同的参数合并顺序，并只移除分页参数。
- 修复 V2 丢失关联字段完整路径造成的「统计字段不存在」。
- 增加关联数值和日期字段聚合、多级及一对多路径展开，并校验每层字段权限和目标记录数据范围。
- 修复关联非空计数错误使用主表外键，改为统计实际路径末端值。
- 修复计数绕过字段权限检查、无效 JSON 返回非预期错误的问题。
- 修复当前页数值字符串极值比较，以及空字符串、布尔值被误计入数值平均值。
- 修复同一字段不同统计方式相互覆盖、日期字段计数被显示为日期的问题。
- 修复原地修改行数据后漏刷新，以及切换统计范围后残留加载/错误状态。
- 修复顶部切回底部时 tfoot 位置判断、异常精度导致格式化报错；支持科学计数法 step。
- 新增回归测试。关联全量统计设有明确处理上限，详见 README。
- 服务端统计错误提示支持中英文，按请求语言返回（原先固定中文）。

## 1.0.0 - 2026-09-21

首个正式版本，支持 NocoBase 2.2.10 及以上的 V1 与 V2 页面。

### 功能

- 统计栏：在表格区块顶部或底部展示统计行，不受区块高度与固定表头设置限制。
- 统计方式：计数、非空计数、合计、平均值、最大值、最小值；按字段类型限定可选范围。
- 统计范围：全部符合条件的数据（服务端聚合，忽略分页）或当前页（浏览器端计算）。
- 展示形式：数值在上、统计方式在下；首列展示统计栏名称。
- 统计行高亮：不高亮、主题高亮色、自定义颜色（按背景亮度自动选择深色或浅色文字）。
- 显示精度：自动（跟随字段精度）或固定 0 / 1 / 2 / 3 / 4 / 6 位小数。
- 列对齐：左侧辅助列（选择列 / 序号列 / 拖拽列）与单元格数量均以表格实际渲染结果（表头 / colgroup）为准，删除列、增删或移动操作列时统计行都不会错位。
- 统计栏名称：仅显示在「序号列」（序号关闭时不显示），不再与第一个数据列的数值合并。
- 多语言：内置简体中文与英文文案。
- 自动刷新：筛选、分页、新增、删除与行内编辑后重新计算统计值。

### 权限与安全

- 统计动作映射到集合的查看（view）权限，复用 ACL 数据范围、字段白名单与当前用户变量。
- 服务端仅接受白名单聚合方式与合法字段名，校验字段存在性与字段类型，单次请求最多 50 个字段。
- 统计失败不影响表格正常加载。

### 兼容性

- NocoBase 2.2.10 ~ 2.2.x（V1 与 V2 页面）
- React 18、antd 5、Formily 2

### 许可

- 采用 Apache License 2.0 开源：可自由使用、修改与商用，需保留版权声明与许可协议，修改或分发时注明来源。

### 说明

- 0.1.x 为内部开发版本，未对外发布，其功能已全部包含在 1.0.0 中。
